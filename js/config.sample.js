/*
  Sample runtime config.
  Copy this file to `js/config.js` and replace the placeholder with your API key for local testing only.
  IMPORTANT: do NOT commit `js/config.js` — keep secrets out of the repo.
*/

window.__CONFIG__ = window.__CONFIG__ || {};
// Example: window.__CONFIG__.AMP_CID_API_KEY = "AIza...";
window.__CONFIG__.AMP_CID_API_KEY = "REPLACE_WITH_API_KEY";
